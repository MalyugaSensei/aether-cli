import type { Route } from "../app/router";
import { sendJson, HTTP_STATUS } from "../app/http";

export const healthRoutes: Route[] = [
  {
    method: "GET",
    path: "/health",
    handler: (_req, res) => {
      sendJson(res, HTTP_STATUS.OK, { status: "ok" });
    },
  },
];
