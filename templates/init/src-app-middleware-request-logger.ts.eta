import type { Middleware } from "./types";
import { logger } from "../logger";

export const requestLogger: Middleware = async (req, _res, next) => {
  logger.info(`${req.method ?? "GET"} ${req.url ?? "/"}`);
  await next();
};
