import type { <%= it.entityPascal %> } from "./<%= it.resourceKebab %>.types";

export interface <%= it.entityPascal %>Repository {
<% if (it.crud) { %>
  findAll(): <%= it.entityPascal %>[];
  findById(id: string): <%= it.entityPascal %> | undefined;
  create(input: Omit<<%= it.entityPascal %>, "id">): <%= it.entityPascal %>;
  update(id: string, input: Partial<Omit<<%= it.entityPascal %>, "id">>): <%= it.entityPascal %> | undefined;
  delete(id: string): boolean;
<% } %>
}
